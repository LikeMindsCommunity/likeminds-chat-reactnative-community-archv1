import Db from "./Db/db";

class Queries {
  // public getModelAData(filterValue: string | null = null) {
  //   let filter = '';
  //   if (filterValue) {
  //     filter = `two = "${filterValue}"`;
  //   }
  //   return Db.getInstance().query('ModelA', filter);
  // }

  // Other query methods
}

export default new Queries();
